-- Dates: SYSDATE(), NOW(), CURDATE()
SELECT NOW();

--sysdate()
SELECT SYSDATE();

--CURDATE()
SELECT CURDATE();
--All the same, except the last only contains the date.
SELECT NOW(), SYSDATE(), CURDATE(); 

